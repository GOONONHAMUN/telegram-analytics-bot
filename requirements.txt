Flask==2.3.3
pyTelegramBotAPI==4.30.0
# Убираем telethon - слишком тяжелый для Render Free
# telethon==1.34.0  # КОММЕНТИРУЕМ или УДАЛЯЕМ
# python-dotenv==1.0.0  # можно оставить если используете .env
